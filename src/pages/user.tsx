import type { NextPage } from "next";
import { Layout } from "src/layout";

const User: NextPage = () => {
  return (
    <Layout>
      <div>user page</div>
    </Layout>
  );
};

export default User;
